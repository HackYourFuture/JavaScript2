'use strict';
// paste your freeCodeCamp solutions in here
//Question. Create a constructor, Dog, with properties name, color, and numLegs that are set to a string, a string, and a number, respectively.
function Dog() {
  this.name = 'figi',
    this.color = 'brown',
    this.numLegs = 4;
}
let hound = new Dog();

console.log(hound);
