'use strict';

function Dog(name, color, num) {
  this.name = name;
  this.color = color;
  this.numLegs = num;
}

const hound = new Dog('Fino', 'Gray', 4);

// Do not change or remove anything below this line
module.exports = hound;
