'use strict';

function Dog(name, color, legs) {
  this.name = name;
  this.color = color;
  this.numLegs = legs;
}

const hound = new Dog('Caramel', 'Beige', 4);

// Do not change or remove anything below this line
module.exports = hound;
